# DocuShield AI — Python Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: EasyOCR will download model weights (~100MB) on first run.
# Models are cached in ~/.EasyOCR/model/
# If you prefer Tesseract, also install the Tesseract binary:
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
#   Linux:   sudo apt install tesseract-ocr

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6

# Image processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# OCR engines
easyocr>=1.7.0
# pytesseract>=0.3.10  # Optional fallback — uncomment if Tesseract binary is installed

# Testing (dev only)
pytest>=7.0.0
httpx>=0.24.0
